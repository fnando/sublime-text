<snippet>
  <content><![CDATA[
Module("$1", function(${2:${1/^(?:[^.]+\.)*(.*?)$/\1/}}){
  $2.fn.initialize = function(container, dataset) {
    this.container = container;
    this.dataset = dataset;
  };

  $2.fn.setup = function() {
    $0
  };
});
]]></content>
  <tabTrigger>comp</tabTrigger>
  <description>Define a new Module</description>
  <scope>source.js</scope>
</snippet>
